export class user {
    name: string;
    lastName: string;
    role: number;

    constructor(name: string, lastName: string, role: number) {
        this.name = name;
        this.lastName = lastName;
        this.role = role;
    }
}